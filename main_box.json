{
  "builders": [{
    "type": "amazon-ebs",
    "access_key": "{{user `aws_access_key`}}",
    "secret_key": "{{user `aws_secret_key`}}",
    "region": "us-west-2",
    "source_ami": "ami-bf8e20df",
    "instance_type": "t2.nano",
    "ssh_username": "root",
    "ami_name": "Main Box {{timestamp}}"
  }]
}
